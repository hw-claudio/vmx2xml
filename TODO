=== NEXT STEP ===

implement testboot_xml.py fully from the sketch.


=== UNSOLVED QUESTIONS ===

*) How to deal with multiple OS disks in the same VM?


=== OTHER DETAILS ===

*) "cputune": vmx sched.cpu.affinity => translate to virt-install "cputune" ?
*) queues=NN performance improvement for disks needs libvirt 10 and qemu 9
*)  --features apic.eoi=on Enable ? APIC PV EOI
*)  --features pvspinlock=on Notify the guest that the host supports paravirtual spinlocks
*) --pm suspend_to_mem.enabled=on,suspend_to_disk.enabled=off
*) --boot cdrom,fd,hd,network translated from VMX bios.bootOrder
*) add bios menuitems? --boot bootmenu.enable=on,bios.useserial=on
*) --network add virtualport.* options?
   https://libvirt.org/formatdomain.html#elementsNICS
   there are VMX vswitch fields to convert from.
*) vdagent for copy/paste with vnc
*) install qemu-guest-agent? install spice-vdagent for VNC clipboard?
*) option to cat /sys/devices/system/node/node1/cpumap > /sys/kernel/debug/tracing/tracing_cpumask ?





